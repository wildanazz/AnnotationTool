### Added

- Several bounding boxes can be selected on the canvas with `Ctrl` + click (or all at
  once with `Ctrl+Shift+A`) and dragged together as a group, or deleted together with the
  delete object shortcut; the whole move or removal is a single item in the annotations history
